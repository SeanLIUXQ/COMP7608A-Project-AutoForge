{
  "trace_id": "4e98b8a0d5644367ae62b588b4baaf8b",
  "timestamp": "2026-05-02T07:22:21.117887+00:00",
  "status": "success",
  "attempts": 1,
  "summary": {
    "headline": "Forge succeeded for moving_average after 1 attempt(s).",
    "function_name": "moving_average",
    "status": "success",
    "attempts": 1,
    "step_count": 5,
    "plan_steps": [
      "Validate that series is a non-empty list of numbers.",
      "Validate that window is a positive integer.",
      "Check that window size does not exceed the length of the series, raising an error if it does.",
      "Compute the moving average by sliding a window across the series, taking the mean of each window slice.",
      "Return the list of computed moving averages."
    ],
    "verification_case_count": 3,
    "verification_passed": true,
    "failure_type": null,
    "failure_message": null,
    "failed_case": null
  },
  "request": {
    "query": "Create a reusable function that computes a moving average over a numeric series."
  },
  "plan": {
    "steps": [
      "Validate that series is a non-empty list of numbers.",
      "Validate that window is a positive integer.",
      "Check that window size does not exceed the length of the series, raising an error if it does.",
      "Compute the moving average by sliding a window across the series, taking the mean of each window slice.",
      "Return the list of computed moving averages."
    ],
    "suggested_function_name": "moving_average"
  },
  "source_code": "def moving_average(series: list[float], window: int) -> list[float]:\n    \"\"\"\n    Compute the simple moving average of a numeric series.\n\n    Args:\n        series: A non-empty list of numbers (int or float).\n        window: A positive integer window size.\n\n    Returns:\n        A list of floats representing the moving averages of each\n        window slice. The length is len(series) - window + 1.\n\n    Raises:\n        ValueError: If series is empty, window is not a positive integer,\n                    or window is larger than the series length.\n        TypeError: If series contains non-numeric values or window is not an integer.\n    \"\"\"\n    # Validate series is a non-empty list of numbers\n    if not isinstance(series, list) or len(series) == 0:\n        raise ValueError(\"series must be a non-empty list of numbers\")\n    \n    # Ensure all elements are numeric (int or float)\n    for i, val in enumerate(series):\n        if not isinstance(val, (int, float)):\n            raise TypeError(f\"All elements in series must be numeric; got {type(val).__name__} at index {i}\")\n\n    # Validate window is a positive integer\n    if not isinstance(window, int) or isinstance(window, bool):\n        raise TypeError(f\"window must be an integer, got {type(window).__name__}\")\n    if window <= 0:\n        raise ValueError(\"window must be a positive integer\")\n\n    # Check window size does not exceed series length\n    n = len(series)\n    if window > n:\n        raise ValueError(f\"window size ({window}) cannot be larger than series length ({n})\")\n\n    # Compute the moving average using a sliding window sum\n    # Use a running sum to avoid repeated sum() calls for better performance\n    result = []\n    current_sum = sum(series[:window])\n    result.append(current_sum / window)\n\n    for i in range(window, n):\n        current_sum += series[i] - series[i - window]\n        result.append(current_sum / window)\n\n    return result",
  "function_name": "moving_average",
  "verification": {
    "success": true,
    "stdout": "[10.0, 11.0, 12.0, 13.0]\n",
    "stderr": "",
    "execution_time_ms": 1894.2031000042334,
    "failure_type": null,
    "cases": [
      {
        "case": 1,
        "input": {
          "__function_name": "moving_average",
          "series": [
            5,
            1,
            9,
            2
          ],
          "window": 3
        },
        "success": true,
        "stdout": "[5.0, 4.0]\n",
        "stderr": "",
        "failure_type": null,
        "result": [
          5.0,
          4.0
        ]
      },
      {
        "case": 2,
        "input": {
          "__function_name": "moving_average",
          "series": [
            1,
            2,
            2,
            3,
            1,
            4
          ],
          "window": 2
        },
        "success": true,
        "stdout": "[1.5, 2.0, 2.5, 2.0, 2.5]\n",
        "stderr": "",
        "failure_type": null,
        "result": [
          1.5,
          2.0,
          2.5,
          2.0,
          2.5
        ]
      },
      {
        "case": 3,
        "input": {
          "__function_name": "moving_average",
          "series": [
            10.0,
            11.0,
            12.0,
            13.0
          ],
          "window": 1
        },
        "success": true,
        "stdout": "[10.0, 11.0, 12.0, 13.0]\n",
        "stderr": "",
        "failure_type": null,
        "result": [
          10.0,
          11.0,
          12.0,
          13.0
        ]
      }
    ]
  },
  "error_type": null,
  "error": null,
  "tool": {
    "tool_id": "bccd01ad-5f87-47bc-b942-f0da1d8e7904",
    "name": "moving_average",
    "description": "Compute the simple moving average of a numeric series.",
    "parameters": [
      {
        "name": "series",
        "type": "array",
        "description": null,
        "required": true
      },
      {
        "name": "window",
        "type": "integer",
        "description": null,
        "required": true
      }
    ],
    "source_code": "def moving_average(series: list[float], window: int) -> list[float]:\n    \"\"\"\n    Compute the simple moving average of a numeric series.\n\n    Args:\n        series: A non-empty list of numbers (int or float).\n        window: A positive integer window size.\n\n    Returns:\n        A list of floats representing the moving averages of each\n        window slice. The length is len(series) - window + 1.\n\n    Raises:\n        ValueError: If series is empty, window is not a positive integer,\n                    or window is larger than the series length.\n        TypeError: If series contains non-numeric values or window is not an integer.\n    \"\"\"\n    # Validate series is a non-empty list of numbers\n    if not isinstance(series, list) or len(series) == 0:\n        raise ValueError(\"series must be a non-empty list of numbers\")\n    \n    # Ensure all elements are numeric (int or float)\n    for i, val in enumerate(series):\n        if not isinstance(val, (int, float)):\n            raise TypeError(f\"All elements in series must be numeric; got {type(val).__name__} at index {i}\")\n\n    # Validate window is a positive integer\n    if not isinstance(window, int) or isinstance(window, bool):\n        raise TypeError(f\"window must be an integer, got {type(window).__name__}\")\n    if window <= 0:\n        raise ValueError(\"window must be a positive integer\")\n\n    # Check window size does not exceed series length\n    n = len(series)\n    if window > n:\n        raise ValueError(f\"window size ({window}) cannot be larger than series length ({n})\")\n\n    # Compute the moving average using a sliding window sum\n    # Use a running sum to avoid repeated sum() calls for better performance\n    result = []\n    current_sum = sum(series[:window])\n    result.append(current_sum / window)\n\n    for i in range(window, n):\n        current_sum += series[i] - series[i - window]\n        result.append(current_sum / window)\n\n    return result",
    "json_schema": {
      "name": "moving_average",
      "description": "Compute the simple moving average of a numeric series.",
      "parameters": {
        "type": "object",
        "properties": {
          "series": {
            "type": "array",
            "description": ""
          },
          "window": {
            "type": "integer",
            "description": ""
          }
        },
        "required": [
          "series",
          "window"
        ]
      }
    }
  }
}