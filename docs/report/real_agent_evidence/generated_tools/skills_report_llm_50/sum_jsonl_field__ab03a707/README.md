# sum_jsonl_field

Parse a string of JSON lines (or an iterable of such lines) and return

## Parameters
- jsonl_text (string) [required]
- field_name (string) [required]

## Imports
json, typing

## Usage
Use `example_input.json` as a starting point for invocation.
