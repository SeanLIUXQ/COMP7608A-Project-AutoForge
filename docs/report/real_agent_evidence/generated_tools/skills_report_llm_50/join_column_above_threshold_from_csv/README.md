# join_column_above_threshold_from_csv

Parse a CSV string, filter rows where a numeric column exceeds a threshold,

## Parameters
- csv_text (string) [required]
- threshold (integer) [required]
- name_column (string)
- score_column (string)

## Imports
csv, io, typing

## Usage
Use `example_input.json` as a starting point for invocation.
