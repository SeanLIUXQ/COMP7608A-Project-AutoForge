# sum_numeric_field_from_jsonl

Parse JSONL input (a newline-separated string or a list of JSON strings),

## Parameters
- jsonl_text (string) [required]
- field_name (string) [required]

## Imports
json, typing

## Usage
Use `example_input.json` as a starting point for invocation.
