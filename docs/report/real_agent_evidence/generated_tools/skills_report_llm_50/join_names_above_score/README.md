# join_names_above_score

Parse a CSV string, filter rows where the score is strictly greater than

## Parameters
- csv_text (string) [required]
- threshold (number) [required]
- name_field (string)
- score_field (string)

## Imports
csv, io, typing

## Usage
Use `example_input.json` as a starting point for invocation.
