{
  "name": "deduplicate_preserve_order",
  "description": "Remove duplicates from an iterable while preserving the order of first occurrence.",
  "parameters": {
    "type": "object",
    "properties": {
      "sequence": {
        "type": "string",
        "description": ""
      }
    },
    "required": [
      "sequence"
    ]
  }
}